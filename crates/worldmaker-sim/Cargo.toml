[package]
name = "worldmaker-sim"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Stage pipeline scaffolding and simulation stages for WorldMaker"

[dependencies]
worldmaker-core = { workspace = true }
rayon = { workspace = true }
anyhow = { workspace = true }
log = { workspace = true }
rand.workspace = true

[dev-dependencies]
# WO-0003 Fix 2 dev-only tests: results-JSON writer for the committed
# plate-generator measurements (internal workspace edge, acyclic), and PNG
# output for the judge-panel plate maps (approved workspace dep).
worldmaker-io = { workspace = true }
serde_json = { workspace = true }
image = { workspace = true }
