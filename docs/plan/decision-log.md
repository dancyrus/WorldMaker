# Decision log

One line per decision: date, the decision, why/scope, and who made it
([Dan] or [default] = Claude's call under standing authority).

| Date | Decision | By |
|---|---|---|
| 2026-08-19 | Core loop is staged simulation with painting at any stage; downstream stages recompute around edits. | Dan |
| 2026-08-19 | Science approach: grounded approximations validated against Earth. | Dan |
| 2026-08-19 | Tech stack: Rust + wgpu + egui. | Dan |
| 2026-08-19 | Globe and flat canvases from day one, with selectable projections. | Dan |
| 2026-08-19 | Timeline at the bottom of the window as the era picker. | Dan |
| 2026-08-19 | Direct plus intent brushes; each stroke hard or soft. | Dan |
| 2026-08-19 | Regions in high detail are the real goal; regional refinement is Phase 5. | Dan |
| 2026-08-19 | Named branches with side-by-side compare. | Dan |
| 2026-08-19 | Three switchable map styles: atlas, satellite, parchment. | Dan |
| 2026-08-19 | Auto-generated editable names. | Dan |
| 2026-08-19 | Magic toolkit stays on the backlog. | Dan |
| 2026-08-19 | Performance benchmarked to the PC only (i7-12700KF, RTX 3080); M1 Air requirement excluded for now. | Dan |
| 2026-08-19 | Repo name WorldMaker, public, MIT license. | default |
| 2026-08-19 | Geodesic grid presets L6/L7/L8/L9. | default |
| 2026-08-19 | Earth-test regression gate from Phase 3. | default |
| 2026-08-19 | Dependency addition: flexi_logger — the approved list allows "log plus a file logger"; flexi_logger provides rotating file logs beside the executable. | default |
| 2026-08-19 | Dependency addition: pollster — one-function futures executor needed to call wgpu's async adapter probe synchronously at startup; no runtime, no transitive weight. | default |
| 2026-08-19 | Dependency addition: bytemuck — safe byte-casting for GPU vertex/uniform buffers; already in the dependency tree via egui/wgpu, standard for wgpu apps. | default |
| 2026-08-19 | Noise stage reseeded through sub_rng(seed, stage-id, purpose) instead of an ad-hoc XOR constant, honoring the Stage RNG contract before Phase 1 copies it; elevation golden hash regenerated (review finding). | default |
| 2026-08-19 | FieldStore gains a u32 integer-field store (plate_id, crust_type, feature bitmask) alongside f32; exact bit ops beat float-encoded ids. | default |
| 2026-08-19 | Sim path bans libm transcendentals: worldmaker-core::dmath supplies fixed-order Taylor sin/cos (small angles), Irwin–Hall gaussians, cube-rejection unit vectors, raw-bit uniforms; exp(-dt/200) is a precomputed literal. Cross-platform golden hashes depend on this. | default |
| 2026-08-19 | Tectonics RNG purposes embed the absolute step index (and plate id), so a run resumed from a keyframe replays identical randomness. | default |
| 2026-08-19 | Keyframes are exact full state: the sim round-trips its own per-cell state through the u16/i16 keyframe quantization at every keyframe (round-then-clamp, idempotent), and per-plate state carries pending sub-cell rotation plus boundary stats — resume-from-keyframe is bit-exact (tested). | default |
| 2026-08-19 | Advection ownership by forward-scatter claims (atomic OR bitmask) + per-cell coverage gather; handles multi-cell sweeps per step. Sub-cell motion banks into a per-plate pending rotation committed at ~0.75 cell so slow plates never freeze to the grid (design-review findings). | default |
| 2026-08-19 | Phase 1 tectonic constants (design review + tuning): slab-pull gain 1.0; collision damping 1.0 saturating at max(5% boundary, 4 cells) with speed floor collapsing to 0 — colliding plates stall (continental-area conservation); speed relax 0.15 up / 0.5 down; pole walk sigma 0.6 deg/step; arc growth 0.6 (ocean) / 0.15 (cont) km/My, cap 70 km, island-arc conversion at 20 km; collision thickening 0.12 km/My per cm/yr; hotspot buildup 0.8/0.4 km/My, cap 8 km, decays with the 200 My constant; trench blend 75% toward -8500 m; arc relief +2000 m; detail noise ±300 m; continental crust fraction = land fraction × 1.35. | default |
| 2026-08-19 | Continental crust thinner than 30 km is subductible (arc terranes recycle); continent ≥ 30 km never consumed — continent-continent overlaps jam in place instead. | default |
| 2026-08-19 | Rift timer: +dt on continent-continent divergence, decays at 2× otherwise (hysteresis vs classification noise); suture timer accrues on any slow (<0.5 cm/yr) continent contact and resets on fast convergence. | default |
| 2026-08-19 | Advection events gated by previous-step boundary class: transform-only cells make no ridge crust on gaps and no trench/subduction on overlaps (hex-zigzag artifact suppression). | default |
| 2026-08-19 | Breakup also triggers when a plate holds > 1/3 of the world's continental crust (not only 1/3 of the sphere, which a floor-6 stalled world never reaches); without it the Wilson cycle gridlocks. Deviation from WO wording, same intent. | default |
| 2026-08-19 | Orogenic relaxation exempts primordial crust (orogeny_age > 1200 My): cratons and painted continents keep their profiles; collision resets orogeny_age so real orogens still decay. | default |
| 2026-08-19 | Keyframe cadence 10 My (spec) at L6/L7, 20 My at L8 to keep a 2 Gy history inside the 1 GB budget. | default |
| 2026-08-19 | Pipeline marks a stage dirty before running it, so a cancelled/failed run can never serve a stale cache entry (design-review finding, regression-tested). | default |
| 2026-08-19 | "Set as present" is applied by the app decoding the chosen keyframe into the world fields — never a re-simulation; the stage itself always publishes the final keyframe. | default |
| 2026-08-19 | Rendering: per-cell layer colors baked on the CPU (layers.rs) into one RGBA8 storage buffer; shader palettes removed. A scrub/layer/sea-level change is a rebake (~1 ms at L7) plus one buffer write; palettes are testable Rust. | default |
| 2026-08-19 | Colormaps: crust age = viridis, thickness = batlow (verified perceptually-uniform anchor tables); elevation keeps the Phase 0 hypsometric ramp; plates use a 24-color categorical palette with a distinctness unit test. | default |
| 2026-08-19 | Plate-boundary "lines" render as one-cell bands colored by classified type (ridge red, trench navy, transform gold) — 56 km wide at L7; true polylines deferred until a phase needs them. | default |
| 2026-08-19 | Plate drag queued as the first item of Phase 2 (WO-0002 allows it): a correct version needs a hashed drag-overlay replayed through the stage cache contract, per the design review. The bit-exact resume machinery it needs (ResumeFrom) is built and tested. | default |
| 2026-08-19 | Arc acceptance metric reads as "a trench on the arc's own (overriding) plate within 400 km" via local search — the globally nearest trench can belong to a neighboring subduction zone without invalidating the arc. | default |
| 2026-08-19 | Craton paint is stored per grid level (cell ids are level-specific): preset changes clear it; seed changes keep it, so painted continents ride along on a reroll. Hotspot placements are unit vectors and survive preset changes (code-review finding). | default |
| 2026-08-19 | Code-review pass (6 finder + 9 verifier agents; 7 confirmed findings, all fixed): dead hover readout, hotspot click during a run could wipe the generated set, craton stroke released off-map skipped its re-run, stability gate was tautological, resume/cancel tests under-asserted. | default |
| 2026-08-19 | Stability acceptance additionally gates continental-crust inventory drift over the run (≤5% relative) — the land-fraction gate alone is pinned by the sea-level solve and proves nothing about the sim. | default |
| 2026-08-19 | Elevation is derived at keyframe steps only: it is a pure function of crust state and nothing reads it between keyframes ("each step" in the WO wording is honored in effect, cheaper in practice). | default |
| 2026-08-19 | Age-depth metric also excludes cells with >0.1 km hotspot buildup (the sub-flag skirt of a shield) — "hotspot cells" by relief, not only by flag. | default |
| 2026-08-19 | The simulated span rounds to whole keyframes, so no steps are simulated past the last snapshot. | default |
| 2026-08-19 | Plates layer colors index by the rank of alive plate ids in the keyframe (≤24 alive → never a collision), not raw id mod 24 — breakup-minted ids no longer alias colors. | default |
| 2026-08-19 | Sea level: solved once at the t=0 anchor (land fraction exact there), then the datum stays fixed so sea level drifts with the hypsometry — coastlines flood and drain through history. Measured: land 0.290→0.341 over 2 Gy at L6, stabilizing; dynamics bit-identical (plate golden unchanged); elevation golden regenerated. Stability gate now checks the anchor land fraction + continental-inventory drift; the over-run land range is recorded as data. | Dan |
| 2026-08-25 | Mac interim: Dan's PC is packed for a move until ~mid-September 2026; the M1 MacBook Air is the primary dev machine. Numbers rule 8 or open orders benchmark "on Dan's PC" are recorded on the Air instead, machine-labelled (Daniels-MacBook-Air), as records not pass/fail gates; PC-gated numbers are re-measured when the PC returns. WO-0003 preset decisions stand; its session records Air timings. macOS CI stays informational. Verified: full suite + all goldens pass bit-for-bit on Apple Silicon; sim wall times ~15% faster than the PC, rendering 7–25x slower in fps but ≥55 fps everywhere. | Dan |
| 2026-08-25 | WO-0003 shape-only partition exception: Track C ships the A-owned frozen A↔C interface as compiling stubs — worldmaker-io strokes.rs (Stroke/StrokePayload/StrokeTool + root re-export) and app pending_edits.rs (overlay constants, OverlayInput, no-op apply_overlay) plus the `mod pending_edits;` line in main.rs — so the B→C→A merge order always compiles; shapes are frozen per feel-pass-design.md § D1 until A's rebase fills the behavior. | default |
| 2026-08-25 | STANDING interaction contract for every brush in every phase (WO-0003 Fix 1): pending edits are an ordered list of strokes — Stroke = { tool, payload }, payload CratonPaint { cells, ±1 } / HotspotAdd { unit } / HotspotRemove { unit }, serde, in worldmaker-io. No stroke may trigger simulation; the guard is structural (stroke machinery lives in worldmaker-io, which cannot name the sim crate — crate wall + lexical + call-site-count tests). Badge counts committed strokes; Cmd/Ctrl+Z pops the newest pending stroke (live stroke first, no redo), mirrored by an Undo button; Discard clears the pending list only. Regenerate folds strokes into TectonicsParams craton_overlay/hotspot_overlay, clears the list, and runs history off-thread with existing progress + cancel. Preset switch discards pending craton strokes (cell ids are per-level) and keeps pending hotspot strokes (positions survive); seed change keeps everything pending. Pending strokes render as tint + outline with the current world behind, via the frozen apply_overlay interface. Pure view controls stay live, never edits: sea level, layers, projections, timeline scrubbing, Detail. | Dan |
| 2026-08-25 | Hotspot remove-matching rule changed from first-within-300km-in-Vec-order to NEAREST within 300 km, ties → lowest index (matches user intent; old order-dependence was only load-bearing through the severed stroke→sim route). Brush geometry now computes thresholds via dmath::det_sin_cos and dots via dmath::dot3 instead of std cos / ad-hoc arithmetic — a brush-edge cell within ~2e-6 of the threshold can flip vs before (no golden exposure: goldens carry no overlays); fold output feeds params_hash, so the stroke path is bit-stable across platforms. Fold base = applied hotspot overlay, else the last completed run's hotspot set (captured at job completion); recording is refused before the first run ever completes. | default |
| 2026-08-25 | "Clear craton paint" and "Reset to generated" are staged-param edits, not strokes: they discard the matching pending strokes AND clear the applied overlay (craton map / hotspot overlay = None), set needs_bake, run NO job, and are not undoable — the world effect lands at the next Regenerate. Pinned consequence (d1-F2): after Reset, the last-run hotspot set is still displayed and still anchors classification/fold, so a hotspot stroke recorded after Reset re-anchors to that set — Reset holds only if no hotspot stroke follows before Regenerate. | default |
| 2026-08-25 | WO-0003 Fix 3 leg 2 — REVERSAL of "per-cell layer colors baked on the CPU" (2026-08-19): layers.rs now bakes per-cell VALUES (f32 scalar + category word); palettes live in a 256×8 Rgba8Unorm GPU LUT baked from the same ΔE-verified Rust anchors (tested texel-for-texel, ocean row sqrt-warped), read per fragment with manual sRGB-space mixes on BOTH canvases via one shared resolve_fragment. Sea level and Detail are live uniforms — dragging them never rebakes. The globe draws unindexed (corner-fetch) so plates are crisp true-cell shapes; legacy boundary bands are debug-only. | default |
| 2026-08-25 | WO-0003 Fix 3 leg 2 implementation notes: (1) d3a §3.2/§4.6's binding tables are realized with SHARED slots for shared resources (positions 1, values 3, overlay 4, palette 5) because one WGSL module serves both pipelines — per-canvas resources reuse slots 0/2; (2) graft 6's per-octave fade is implemented as length(fwidth(q)) — the literal fwidth(length(q)) is identically ~0 on the sphere (|q| = freq) — intent kept, letter corrected; (3) provisional detail defaults octaves 5 / A0 220 m until leg 4's sweep. | default |
| 2026-08-25 | WO-0003 Fix 3 — Eckert IV constants (D4 entry): aspect() = 2.0 exact; CPU forward Newton from θ₀ = φ/2 computed internally in f64, cap 12, tol 1e-9 rad, pole short-circuit at \|sin φ\| ≥ 1−1e-7; inverse closed-form on both CPU and WGSL (misc.x = 2.0) under the shared π·1.0001 hair gate, with a transliteration test pinning accept/reject parity + 1e-6 value agreement between the WGSL arm and core invert. | default |
| 2026-08-25 | WO-0003 Fix 3 leg 3 implementation notes: (1) A3's mirror test asserts the ≤1e-4 weight bound against an independent f64 raw-Cramer solve of the mirror's OWN wedge triple, and wedge equality vs the f64 containment reference except where that reference itself measures the point inside the f32 noise band (triple product ≤ 1e-6) of the shared boundary plane — at L9 the f32 g-test genuinely fuzzes there and either adjacent wedge is valid (the interpolant is continuous across the plane), while a real B1-style sign error still fails both branches; (2) both uniform blocks grew a bnd vec4 (globe 128 B, flat 96 B vs d3a §6's 112/80) — the boundary-ribbon VS needs the canvas rect and ribbon half-width in pixels — and flat binding 0 became VERTEX_FRAGMENT; (3) BoundaryChain carries a `closed` flag, back-hemisphere culling is the FS-discard option of A9, and a crossing edge with no F_BND bit that keyframe draws as transform. | default |
| 2026-08-26 | L9 keyframe cadence = 100 My (new keyframe_interval_my arm; L6/L7 goldens and the L8 20 My arm untouched, L5 pin unaffected): at 16 B/cell a 2.62 M-cell L9 keyframe is ~42 MB, so 20 My would cost ~4.2 GB over 2 Gy vs the 1 GB budget; 100 My keeps a max-span Ultra history at ~0.88 GB, the same ballpark as L8's ~1.06 GB. DT stays 2 My (wall time unchanged); histories carry their own interval, so the era picker needs no app change. | default |
| 2026-08-26 | WO-0003 Fix 2 plate-generator competition judged (3 lens subagents + synthesizer; record: docs/plan/feel-pass-design/plate-judge-record.md; numbers: docs/results/plategen-feelpass-Daniels-MacBook-Air.json; curated PNGs: docs/media/feel-pass/plate-panel/). WINNER: hybrid, unanimous — totals hybrid 24, growth 20, warped 12, incumbent 4. Hybrid has zero true enclaves across the panel (the flagged seedcyrus teal region touches two plates — synthesizer pixel adjudication); growth has two plus a recognizable polar-supergiant fingerprint. FINAL gates on the gate triple (L7 s42 + L6 s7 + L6 scyrus): CV >= 0.50 (formula value stands; incumbent best 0.1361, hybrid worst 0.7210), sinuosity >= 1.18 — raised above the formula floor 1.1665 with logged reasoning: it clears the incumbent best anywhere (1.1545, L6 s271828) by >= 0.0255, floor 1.1665 is barely distinguishable from the ~1.10–1.15 hex-zigzag baseline, and hybrid keeps >= 0.0495 margin at its triple worst 1.2295. Formula crossing occurred for growth only; selecting hybrid dissolves it. Polar-row streaking on the panel PNGs is equirect projection stretch (single polar cells smear across columns; nearest_cell mapping is exact), shared across generators — render-side, non-blocking, noted per record condition 2. | default |
| 2026-08-26 | WO-0003 Fix 2 P2 pre-wiring checks (hybrid, per judge record §4 and F8 uncommitted wiring): (1) connectivity — L6 seed7 plate 8 came out in 3 components (the anneal pinches thin lobes; also the source of the "36 boundary segments" flag). The record's fix rule is now a standing deterministic post-pass in the hybrid generator (each minority component reassigned to the neighboring plate with the longest arc_len3 boundary, ties to lowest plate id, iterated to a fixed point). Post-fix all 10 (seed,level) pairs are single-component; only L6 seed7 moved (CV 0.7210->0.7217, sinuosity 1.2296->1.2218, segments 36->30) and the ruled gates (CV 0.50 / sinuosity 1.18) still clear on the gate triple with margins 0.2217 / 0.0418. (2) 2 Gy L6 seed42 stability — FAILED the committed gate: alive plates crash 12->6 by 400 My and pin at the floor (breakups 4 vs baseline 17, sutures 10 vs 23); continental inventory drifts 0.392->0.346 (11.5% rel vs the <=5% gate; anchor land 0.2902 and alive band 6..24 both pass). Consumption of heavy-tailed runts is the driver (risk R1 as designed). M3 (winner wiring + the single golden regen) is HALTED pending a ruling: retune inside the pinned bands (raise f_small toward 3% / lower the giant band edge) would change every map and thus re-open the panel scores and gate numbers. No goldens moved; default generator remains the incumbent. | default |
| 2026-08-26 | WO-0003 Fix 2 stability retune (run 3; orchestrator ruling: hybrid stays the panel winner, fix the 2 Gy stability failure by retuning its area-target law inside the pinned bands — giant 15–25%, runt 1–3%). DIAGNOSIS (instrumented 2 Gy L6 runs): the alive-plate engine at the 6-plate floor is a ~104 My breakup↔suture limit cycle — suturing is gated off at the floor but pair timers keep maturing, so each breakup opens a one-plate window that is immediately spent on the oldest matured pair; breakups in turn need one plate to hold >1/3 of all continental cells (the n/3 sphere-area trigger never fires in practice). The incumbent baseline ALSO crashes 12→6 by ~300 My; it stays healthy because its floor states always hold a cont-share supercontinent and the windows re-merge continental giants, re-concentrating crust for the next breakup. The competition-band hybrid seized at t=586 My: its 1.5–3% runts carry cratons to their rims (craton BFS from a shallow interior), jam on first contact and mature many suture timers; 10 sutures burn the count to the floor, matured runt pairs poach the post-breakup windows, and continental crust ends spread over 4 co-equal plates (max share 29.6% < 1/3) — engine dead, then 1.4 Gy of unopposed margin grinding by the two mobile near-oceanic plates (drift 11.5% vs the ≤5% gate). Zero consumption deaths — R1's plate-death premise was wrong; the count crash is all sutures. SWEEP (15 configs, 2 Gy L6 seed 42; fb-band/fs-band/mid-flat → drift%): .15-.25/.015-.03/0→11.5 (classic); .15-.25/.025-.03/0→10.9; .15-.25/.03/0→13.5; .15-.20/.03/0→25.7; .15-.25/.025-.03/.5→23.1; .15-.18/.03/.7→6.0; .15-.16/.03/1.0→9.7; .15-.17/.03/.85→22.4; .15-.18/.028-.03/.85→28.5; .22-.25/.03/0→4.1 PASS; .20-.25/.025-.03/.3→28.5; .21-.25/.03/0→21.3; .22-.25/.028-.03/0→16.0; .22-.24/.03/0→10.5; .23-.25/.03/0→28.7. CHOSEN: f_big 0.22–0.25 (top of the pinned band), f_small pinned at 0.03, pure geometric ladder, no mid-tail flattening (HYBRID_BANDS in plate_gen.rs; growth/warped keep CLASSIC_BANDS — all 60 committed classic judge-record values re-verified bit-identical after the parameterization). Physics: the top-of-band giant keeps continental crust concentrated so the supercontinent trigger stays reachable (engine cycles t=322–1184 My, 9 breakups vs classic's 4), and 3% runts keep their cratons clear of their margins. Seed 42 (the committed gate): drift 4.10%, alive 6–12, 15 sutures / 9 breakups — PASS. Informal robustness (recorded, not gated): seed 7 → 2.08% (23 sutures/17 breakups) PASS, seed cyrus → 6.21% (8/2) FAIL; for context the INCUMBENT on the same config measures 6.5% (seed 7) and 9.0% (cyrus), and classic hybrid on cyrus even breaches the alive floor (min 5). The ±5% drift gate has only ever been measured at seed 42, and 2 Gy drift is chaotically config- and seed-sensitive for BOTH generators (adjacent sweep configs scatter 4–29%); the retuned hybrid's three-seed profile {4.1, 2.1, 6.2}% beats the incumbent's own {1.2, 6.5, 9.0}% — flagged for the re-judging panel: the gate as committed measures a single seed of a wide distribution. GATES: retuned hybrid clears the ruled gates (CV 0.50 / sinuosity 1.18) on the gate triple with LARGER margins than the competition hybrid — triple CV 0.8280/0.8213/0.9152 (worst margin +0.3213), triple sinuosity 1.2348/1.2831/1.2927 (worst margin +0.0548) — and now clears both gates on all 10 (seed,level) pairs (CV min 0.7450, sinuosity min 1.2023; classic's off-triple sinuosity min was 1.1735 < 1.18). P3 rule bands recomputed for the re-judging panel: CV band [0.50, 0.7713], sinuosity band [1.1665, 1.2148] (ceiling moved up from 1.2095; floor unchanged) — the ruled 0.50/1.18 sit inside both with margin. Single-component connectivity holds on all 10 pairs (standing post-pass). hybrid2_* metric + 2 Gy stability rows added to plategen-feelpass-Daniels-MacBook-Air.json in the same commit; retuned panel PNGs rendered to target/plate-panel/plates-hybrid2-L7-seed{42,cyrus,7,1002,271828}.png beside the originals for side-by-side judging. Goldens untouched (incumbent still wired; workspace suite green incl. tectonics_reproduces_committed_goldens). step.rs untouched — the sanctioned step-3 fallback was NOT needed. | default |
| 2026-08-26 | dmath gained arc_len3 at Fix 2 M1 (row recorded at M3 per the d2 §9 drafts): full-range great-circle length by 4 fixed midpoint-normalize bisections + a fixed-order odd series on the residual chord; only IEEE-exact ops (+,−,×,/,sqrt); argument order canonicalized on f32 bit patterns so symmetry is bit-exact; error ~1e-6 abs typical, degrading ~1.2e-7/(π−θ) near antipodal (F2/F11); tested against f64 acos. The sole distance used by the committed plate metrics — numerator, denominator, incumbent measurement and gates all move together by construction. | default |
| 2026-08-26 | WO-0003 Fix 2 M3: t=0 plate generator REPLACED — setup.rs now calls the retuned hybrid growth+warp (C9: AreaBands giant 22–25% / runt pinned 3%, fBm-warped Dijkstra growth, boundary anneal, single-component post-pass) as the sole generator. Authority: competition panel 3–0 (judge record) + re-judging panel 3–0 on the retune (record addendum). Feel delta on the panel: plate-area CV ~0.08–0.14 → 0.75–0.92, boundary sinuosity ~1.11–1.15 → 1.20–1.29; zero enclaves; the incumbent's soccer-ball fingerprint is gone. Losers and scaffolding deleted per record §5: Incumbent/MultiSeedGrowth/WarpedVoronoi impls, trait PlateGenerator, all_generators, score_generators, the M1 one-shot measure test; plate_gen demoted to private; render_plate_maps kept as a SimState::setup-only dev tool. Full Phase-1 acceptance re-passed with the hybrid wired (all_acceptance_pass = true: age-depth 2.77%, Ashman D 6.53, arc-trench 100%, 2 Gy stability drift 4.1% with alive 6–12, determinism repeat exact). | default |
| 2026-08-26 | WO-0003 Fix 2 M3: BOTH tectonic goldens regenerated — the one sanctioned golden move, exactly once: elevation 0xf751_0e72_14ed_5b62 → 0x7b43_ec03_a6ef_ca2a, plates 0x70df_6db8_ec5f_653d → 0x1690_72d7_7080_3f71. They move together because the craton stream's draw alignment is geometry-entangled with the plate map (whole-world change, expected). Values taken from the M3 harness run per the d-determinism recipe; the feelpass JSON determinism hashes equal the committed constants (cross-checked in-commit); phase-0 noise golden 0xa86a_7471_79a3_5a46 verified UNMOVED. Obligation noted: after Tracks A and C merge, re-run the sim suite and record that the hashes did not move. | default |
| 2026-08-26 | WO-0003 Fix 2 M3: CI feel gates ARMED (plategen_gate_tests.rs always-on, setup-only, ~0.3 s release): plate-area CV ≥ 0.50 and boundary sinuosity ≥ 1.18 on the pinned gate triple (L7 s42, L6 s7, L6 scyrus); constants canonical in tectonics::metrics; the test doc comment quotes the incumbent and retuned-hybrid triple measurements verbatim per the panel ruling. Connectivity (exactly one component per plate) and no-enclave (≥2 distinct neighbor plates) checks promoted into the gate suite on the triple — recommended by both panels, kept because they stay fast and deterministic. The harness echoes the gates and folds plategen_gates_pass into all_acceptance_pass from M3 on. The 2 Gy stability gate stays seed-42-only; multi-seed drift is recorded-not-gated (addendum A5; Phase-2 open question). | default |
| 2026-08-26 | WO-0003 Fix 3 leg 4 presets/flags (D4): default generation preset is High8 (~28 km cells; startup world build measured 8.3 s wall on the Air — L8 grid 81 ms + raster 42 ms + 500 My sim 8.26 s); Ultra9 (L9, 2.6M cells) enabled in the combo; Draft6/Standard7 stay. CLI flags --seed (hashed by the exact seed-box path seed_from_text, so script/UI parity is by construction), --preset draft6|standard7|high8|ultra9, --detail 0..1, dev-only --detail-octaves/--detail-amp-m. Graft 7: screenshot mode with NO explicit world flags forces seed cyrus + Standard7 + detail 1.0 (AFTER matches the committed BEFORE by default), and the parity trio is re-forced when a combined perf+screenshot run chains into its shots. Unknown args stay warn-and-ignore; warnings mirror to stderr and the flag-passing wrapper scripts (scripts/perf-feelpass.sh, scripts/detail-sweep.sh) grep it and fail loudly on "ignoring" (D4 old-binary flag swallowing). | default |
| 2026-08-26 | WO-0003 Fix 3 leg 4 perf methodology: the perf loop (Standard7->High8->Ultra9, per preset Globe/Flat/Split, 40 warmup + 240 sampled, Elevation + Detail 1.0, vsync off) now WAITS for all submitted GPU work once per frame (device.poll Wait; fps_gpu_synced=true in the results JSON). Measured why: with vsync off, a macOS window that is occluded (scripted runs on this Air) never blocks on present, so the unsynced counter measured CPU encode speed — 6,000+ "fps" at L7/L8 where the GPU-synced truth is in the hundreds. Phase-1 fps records were CPU-bound and unaffected, but feel-pass fps files are only comparable to other fps_gpu_synced records. fps_grid_level is retired: every fps/sim-wall key is suffixed _L7/_L8/_L9 (d3a s10.3). | default |
| 2026-08-26 | WO-0003 Fix 3 leg 4 sweep machinery (d3a s12 + grafts 6/8): sweep capture mode = a --screenshots run carrying either dev detail flag; it captures exactly the two judged crops per config — the deterministic coast close-up (graft 8/A9 rule on the final era's keyframe, integer i16-meter slopes, serial id-ordered scan) and the existing mountains stage. Coast crop framing: flat view centered by a deferred pan/zoom resolved against the real canvas rect, COAST_CROP_ZOOM = 12 (~30 deg of longitude across the 1600 pt scripted window, an L8 cell ~13 pt — embellishment legible against facet size). scripts/detail-sweep.sh drives the 24-config matrix (octaves {3,4,5,6} x A0 {120,220,350} m x seeds {cyrus, feelpass}, High8, Detail 1.0) plus an index.md panel table; PNGs are judging scratch, not committed. The default (octaves, A0) pick belongs to the judge panel — provisional 5/220 stands until then. | default |
| 2026-08-27 | WO-0003 Fix 3 leg 4 render-detail default = octaves 5 / A0 350 m (was provisional 5/220), judged inline from the 24-config sweep (octaves {3,4,5,6} x A0 {120,220,350} m x seeds {cyrus,feelpass}, High8, Detail 1.0) against detail-OFF references captured at the same framing. Criterion: coast embellishment visible, no invented mountains. FINDINGS: no config invents mountains — the mountains-crop ridge, snowfields and shapes are identical at every setting (350 m adds only occasional one-cell lakes/islets); coast visibility rises ~linearly with A0 (pixels changed >8/255 vs detail-off, both seeds: A0 120 ~20-29k, 220 ~28-32k, 350 ~52-83k of 4.9M) and is nearly flat in octaves >=4 (the footprint fade already silences sub-pixel octaves at the judged zoom), while octaves 3 concentrates energy in blobby cell-scale features. 350 is the only swept A0 where the coast criterion is defensibly met: the shader's slope conditioning floors flat coasts at 0.30x and normalized fBm typically excursions ~0.25, so effective coastal relief is only ~A0*0.075 (350 -> ~26 m typical) — recorded as a Phase-2 open question (raise the conditioning floor near e=0 or re-map the Detail slider if Dan still reads the default as flat). CAVEAT: one of 24 captures (oct3/220/cyrus coast) shot open ocean — the deferred coast pan resolved after the capture; the re-run matched its family and replaced it. The pan/capture race is real but rare; S4's screenshot step must sanity-check framing before judging AFTER images. | default |
| 2026-08-27 | Coast-crop pan/capture race FIXED (the 1-in-24 sweep flake above): drive_shot now refuses to request a screenshot while a deferred flat centering is pending, and, once the pan is resolved, waits until it has been on screen for >= 1 full frame against an unchanged canvas rect (a rect change — e.g. window still settling — re-resolves the pan against the real rect and restarts the wait). The 30-frame settle becomes a floor rather than a fixed trigger; a pan that never settles is captured anyway after 600 frames (~10 s) with a loud log::error so a scripted run can't hang silently. Manual pan/zoom clears the tracking, so interactive use is untouched. App-only change; no sim path, goldens unaffected. | default |
| 2026-08-27 | WO-0003 Fix 4 (S2) plate-motion liveliness constants, all in step.rs: SPEED_FLOOR_JAMMED 0.05 deg/My (new; floor = SPEED_MIN − (SPEED_MIN − 0.05)·f_coll — the old (1 − f_coll) floor collapsed to exactly 0 and, with COLLISION_DAMP zeroing the slab-pull target, froze jammed plates at 0.00 deg/My forever; measured on v0.2.0-era code: overlap up to 0.999 per 200 My window from ~1200 My, speeds exactly 0.00). COLLISION_DAMP stays 1.0 (floor alone meets the ≥0.05 requirement). SUTURE_SLOW_CMYR 0.5 → 1.2: the 0.05 creep closes a two-jammed-plate contact at up to ~1.1 cm/yr, which 0.5 read as active convergence — timers reset every step and welds ground forever. SUTURE_DECAY_MULT 2.0 (new): fast steps decay the pair timer instead of hard-resetting it; escape flickers had postponed a measured weld's suture ~340 My. Gridlock breaker in maybe_breakup: at PLATE_FLOOR with a matured pair, the most-continental eligible plate (≥32 cont cells, suture age >100 My; max by cont cells, tie to low id) breaks up so the breakup↔suture window cycle runs without a supercontinent (fixes the engine-death mode diagnosed 2026-08-26). SPEED_MAX 1.2 → 2.0, shared with setup's base-speed clamp: at vigor 1.73 the old cap pegged base speeds and every slab-pull target. Liveliness gates canonical in tectonics::metrics (overlap ≥0.985/300 My/≥50 cells; speed <0.05 for >200 My) with logged exemptions (fresh collision ≤30 My; free mover: window-mean speed ≥0.1 AND contact age ≤30 My — spinning microplates and internal-pole giants are moving, not frozen); enforced by liveliness_tests.rs (defaults s42 + echo seed "dan"/8/0.40/1.73, both 2 Gy L6) and harness rows. Acceptance re-run clean, no retune: drift 2.1% (M3: 4.1%), alive 6–12, sutures 35 / breakups 30 (engine ~2× more active), age-depth 3.7%, Ashman 9.17, arcs 100% (tectonics-fix4-Daniels-MacBook-Air.json). | default |
| 2026-08-27 | WO-0003 Fix 4: BOTH tectonic goldens regenerated — the SECOND sanctioned golden move (Fix 2's "exactly once" bookkeeping closed at M3; this one closes Fix 4's), exactly once, as the final commit of the feat/feel-pass-motion branch: elevation 0x7b43_ec03_a6ef_ca2a → 0x857b_8233_0e24_2c03, plates 0x1690_72d7_7080_3f71 → 0xa8c4_9d9b_f779_59e8. They move together because the plate-motion force model changed (jammed floor, suture threshold + hysteresis, gridlock breaker, SPEED_MAX — constants row above), which alters every step of every run from t=0 — a whole-world change by design, not a red flag. Values taken from the Fix 4 harness run (tectonics-fix4-Daniels-MacBook-Air.json), whose determinism hashes equal the committed constants by construction; phase-0 noise golden 0xa86a_7471_79a3_5a46 verified UNMOVED in the same suite run. Obligation carried forward: after Track A merges, re-run the sim suite and record that the hashes did not move. | default |
| 2026-08-27 | Plate physics: model proposed, awaiting Dan's ruling. See plate-physics-model.md (companion audit: plate-physics-audit.md; probe data: docs/results/plate-physics-probe-{cyrus,42}.json). WO-0005 diagnosis: 8 of 30 step.rs mechanics have no physical basis; largest plate reaches 87% of the sphere by 200 My; plate count pinned at the floor with 2/3 of breakups fired by the gridlock breaker; fragmented plates in 20 of 21 samples. No sim code changed in this WO. | Dan |
| 2026-08-27 | WO-0003 S4 close: ten-area verification pass ran against the real build; every area verified, confirmed findings fixed in the close commit. (1) perf-feelpass JSON RE-MEASURED at the shipped detail default A0 350 m — the committed 2026-08-26 file was recorded at the provisional 220 m; fps moved within noise (globe L9 44.96→42.44, split L9 40.05→39.22, all L8-default views ≥59) so Track C's headline claims stand, now with matching backing. (2) Third determinism golden pinned: GOLDEN_TECTONIC_CRUST_TYPE_L6_SEED42 = 0xf771_678a_67d5_19a4 (the harness recorded this hash in every results JSON but no test asserted it); value from the same Fix 4 harness run as the other two constants — this is a new assertion, not a golden move. (3) Eckert IV polar band 89°–90° now tested (polar_band_roundtrip, 5e-4 rad bound): the pole snap makes a ~3 km cap read exactly 90°; documented at eckert4_theta, view-path only. (4) Pending-guard hardening: app-side lexical scan gains build_world/worldgen needles (the app crate CAN link the sim, so its wall is lexical; the scan previously missed the app's actual sim entry point). (5) CLI value-taking flags warn on a missing value (the D4 "ignoring" grep previously missed a truncated invocation). (6) Stale step.rs speed-floor comment corrected (described the pre-Fix-4 collapse-to-zero behavior). Suite re-run at close: green (15/15 binaries), goldens byte-identical — Tracks C and A moved no golden hash after Fix 4's sanctioned move, closing the obligation carried in the two golden-regen rows above. Residual accepted risks logged in WO-0003-feel-pass.md Stage V. | default |
| 2026-08-27 | Plate physics: model accepted with amendments (WO-0006). Dan accepted plate-physics-model.md as the design of record with three amendments: (A) a rift nucleates and advances only where driver stress exceeds local strength — a stalled rift is a failed rift; (B) supercontinent breakup comes from mantle-insulation weakening of the strength field, not from biased hotspots; `BREAKUP_SUTURE_AGE_MY` is retired; (C) slab segments detach individually — continuous subduction keeps a rolling attached slab, and pull fades only after subduction stops. Rulings on the model's open questions: slabs keep pulling after the subducting plate dies (the ledger transfers to the consuming plate); keyframes may grow to 20 B/cell (slab_plate + slab_since_my; re-measure the L7 budget); a long quiet supercontinent is accepted world history; the plate palette is extended rather than capping microplates. Implementation split across WO-0006 S1 (force balance, slab ledger, connectivity), S2 (suture, strength field, rifting), S3 (calibration, gates, goldens, Overlay layer). | Dan |
| 2026-08-27 | Repo gating note (S4 audit): main has NO branch protection — fmt/clippy/test are advisory; rule 1's "merge only when CI is green" is enforced by session discipline, not by GitHub. An attempt to enable required status checks (rustfmt, clippy, tests-release; macOS stays informational; admins exempt so concurrent-session workarounds keep working) was blocked by the session permission layer. Standing recommendation for any future session with the permission: apply exactly that configuration. | default |
| 2026-08-28 | WO-0006 S2 implementation defaults (all S3-calibratable): (a) primordial continental crust (age_ref ≥ 1200 My, the orogeny-relaxation window) keeps S_type 1.0 in the strength field — the model prose anchors cratons at ~2.0 and the WO's craton test requires ≥ 1.5, but the §4 formula's 0.6 continent factor caps continents at 1.2, so the craton exemption reconciles formula with anchor; (b) "nascent ridge": ocean-ocean boundary cells younger than 30 My count as ridge drive (ridge_cells + torque, not classification flags) even below the divergence dead band, so a fresh split corridor supplies ridge push with no imposed speed — this is how the halves of a split start separating; (c) a rift tip walking into plate-interior ocean lays a fresh ridge line (crust_age → 0, F_RIDGE) — the propagating-ridge reading — so the split-corridor test (plate ocean ≤ 60 My) sees the full path; (d) trench-trapped microplates require a purely oceanic fragment ≥ max(12, cells/300) against an active trench — the first cut (any 12-cell fragment) cascaded to 65 alive plates in 200 My at L5; splits and promotions defer above 60 alive plates (advection candidate mask is now u64); (e) driver stresses plume 1.0 / back-arc 0.5 / opposing-slabs 0.7, placeholders shaped by the §4 anchors; (f) suture pair timers hard-reset when any §3 condition lapses ("sustained" read literally; the old decay hysteresis died with SUTURE_DECAY_MULT); (g) keyframes grow 20 → 22 B/cell for the per-cell suture scar (u16, sentinel = never) — L7/2 Gy ≈ 0.72 GB, re-measure in S3; (h) anti-froth dampers, all physical: a 200 My per-plate rift refractory (stress re-accumulation; the plume's residence clock also resets on nucleation — the head has vented), rifts neither nucleate on nor split plates under 2% of the sphere (a splinter deforms instead), and the trench-trapped gate is 1% of the sphere — without these the census railed at the 60-plate mask cap and continents ground to 1% by 2 Gy. Measured after damping (L5/L6 2 Gy debug): census ~23–37 and stable, continents retained BETTER than S1 main (0.11 vs 0.06 sphere fraction at L6/2 Gy — the continental leak is pre-existing S1 behavior, flagged for S3's inventory gate); sutures 0/Gy at L5/L6 defaults because the strict §3 conditions (30% perimeter, ocean-free 2 rings) never co-occur in current geometry — mechanics verified by unit test, frequency is S3 calibration. Also fixed in passing: the §7 backstop's single sweep could strand two adjacent fragments (each targeted from pre-pass ownership, so F1 could join Q through cells of F2 that simultaneously left Q) — present since S1 (its probe shows 1 exclave sample); the sweep now iterates to a fixpoint (≤ 8 passes, normally 2), and the S2 probe records 0 multi-component samples at both seeds. | default |
| 2026-08-28 | WO-0006 S3 calibration (27 recorded trials + final; every trial in docs/results/plate-physics-calibration.json). FINAL coefficients: K_SLAB 0.66, K_RIDGE 0.25, K_MANTLE 0.09, C_DRAG 1.25, C_CONTACT 450, C_TRANSFORM 0.5, TAU_MY 20, SLAB_DETACH_MY 100; strength field: S_type ocean 1.0 / continent 0.78, AGE_REF 350 My, g_age clamp [0.9, 2.0], suture weak floor 0.5 healing over 150 My, thickness ref 20 km, hot-orogen factor 1.0 (calibrated to neutral: at L6 an extra-weak hot orogen is an anti-suture rift magnet), insulation floor 0.18. Residual drift K_MANTLE/C_DRAG = 0.072 deg/My = 0.80 cm/yr (model band 0.3–1). Key mechanism findings: the strength field is nearly bimodal around the fixed driver stresses (0.5/0.7/1.0), so rift activity switches regimes sharply — THICK_REF 20 km puts island-arc crust at full strength (kills the arc-chain rift froth that railed the census), AGE_REF 350 lets plumes cross mid-age crust; opposing-slab rifting localizes on scars and insulated supercontinents only. §9 gate outcomes at the final: ARMED in CI (both seeds green) — m1 plate count, m2 condition audit, m3 split frequency+attribution, m4 share<45% outside epochs, m5a zero exclaves, m7 mean speed 2–6 cm/yr, m8 liveliness; RECORDED-not-armed with structural reasons — m2 frequency (0.5/Gy vs 2–10: relic enclosed basins block §3 condition 3; needs obduction/basin closure), m4 epoch dispersal (greedy min-strength rift walk shaves slivers; 200 My refractory caps pace; needs stress-axis rift pathing), m5b backstop budget (8–10k cells/100 My vs ≤10: forward-scatter advection seam churn; needs connectivity-aware advection), m6 relief (66/68% vs ≥80%: C_CONTACT locks contacts and shortens the convergence window — one knob serves two opposed targets), m7 slab ratio at cyrus (1.90 vs ≥2; seed 42 2.64). | default |
| 2026-08-28 | WO-0006 S3 metric interpretations (all documented in tectonics::metrics): (a) §9's eight items are enforced as nine gates — item 5 splits into 5a zero-exclaves and 5b backstop budget; (b) metric 8's "in a §3-qualifying collision" is read as "in continent-continent contact (carries a §3 pair timer)" — the calibration probe measured every slow span as a contact stall, which §1 endorses and §7 requires; the slow clock pauses (never resets) during contact, so a no-contact freeze is still caught; (c) gate 7.1's free-mover threshold drops 0.1→0.02 deg/My (a frozen plate reads exactly 0.00 under the force balance; transient internal-pole spinners read 0.01+) and its collision exemption covers contact spanning ≥ half the 300 My window; (d) metric 6 counts collisions (contact zones that actually converged past the dead band) and merges same-pair gaps < 50 My (a zone, not a classification streak); (e) metric 4 merges sub-100-My dips below the 1/3 threshold (census flapping is not dispersal); (f) metric 7's slab ratio falls back to the S1 above/below-median attached-area statistic when settled slab-free plates are structurally absent, and the drift band binds only when its population is measurable (≥20 plate-samples). Suture-condition failure counters (extent/lock/ocean) added to SimState as diagnostics. | default |
| 2026-08-28 | ALL THREE tectonic goldens regenerated — the third sanctioned golden move, WO-0006, exactly once: elevation 0x857b_8233_0e24_2c03 → 0x2916_52b9_61e3_f74c, plates 0xa8c4_9d9b_f779_59e8 → 0xa154_6f8c_7944_6284, crust type 0xf771_678a_67d5_19a4 → 0x08f5_c8f9_67db_bc04. They move together because WO-0006 S1–S3 replaced the plate-motion model wholesale (force balance + slab ledger + strength-field suture/rifting + calibrated coefficients), which alters every step of every run from t = 0 — a whole-world change by design, not a red flag. Values from print_tectonic_goldens on Daniels-MacBook-Air at the S3 calibrated constants; the S1 #[ignore] markers removed in the same change; phase-0 noise golden 0xa86a_7471_79a3_5a46 verified UNMOVED in the same suite run. | default |
| 2026-08-28 | WO-0006 S3 Overlay layer + wo6 screenshot mode: Layer::Overlay renders the Plates layer at 40% brightness with each cell whose slab_plate is set drawn in the slab plate's color, faded by (1 − slab age / SLAB_DETACH_MY) — fresh slabs bright, detached slabs gone; slab color index rides category bits 20–27, fade in the scalar, shader layer id 4. `--wo6-shots <dir>` captures the six pinned WO-0006 documentation shots (Split, Eckert IV, 2 Gy span forced; seed/preset from the CLI). L7 keyframe budget re-measured at 22 B/cell: 0.725 GB per 2 Gy — inside the 1 GB budget. | default |
| 2026-08-27 | WO-0008 S0 (Dan's ruling 2026-08-28): at t = 0 every plate is entirely continental or entirely oceanic — the craton area budget (OCEAN_PLATE_CHANCE + weighted per-plate targets) replaced by a greedy whole-plate subset pick closest to the continental budget; cratons survive as one 30–60%-of-plate core per continental plate, the rest is 35 km platform crust aged 200–800 My. Achieved land fraction (quantized by plate sizes) recorded in SimState/TectonicsHistory and labelled in the UI ("target 29% -> start 31%"; ASCII arrow — egui's default font has no U+2192). Tectonic goldens `#[ignore]`d until the S2 regeneration (fourth sanctioned move, announced in the WO). Two gate clauses demoted ARMED→RECORDED with gross-regression backstops until S1 re-arms them: m1's >500 My pin (seed cyrus pins 530 My — whole-plate continents lengthen locked spans) and m3's 2–8/Gy split band (seed cyrus reads 1.5/Gy — large unbroken continental plates rift less often); every other armed clause passes at both seeds. | default |
| 2026-08-28 | WO-0008 S1 (Dan's rulings 2026-08-28): (1) relic-basin closure — locked continent pairs (conditions 1+2, tracked by a new `PairTimer::locked_my`) consume enclosed oceanic basins (border ≥ 80% the two plates) at their margins at the pair's convergence-equivalent rate (mean contact rel speed floored at the dead band), feeding the consuming plate's slab ledger; basins ≤ RELIC_BASIN_KEEP_CELLS = 12 survive as relic seas and suture condition 3 becomes "no oceanic region larger than 12 cells within 2 rings of the contact". (2) Rift linkage: same-plate rift tips within 3 cells connect along the least-strength path and merge. (3) Advection seam tie-breaking reworked so both passes agree (rule recorded in WO-0008-S1 and advect()); backstop budget gate armed at ≤ 10 cells/100 My. (4) Hard continent (≥ SUBDUCTIBLE_CONT_KM) is never deleted at a consuming margin — it transfers to the winner (terrane accretion); continental area at 2 Gy gated to ±15% of t=0; weld gate 2–6/Gy. Sim hashes move; goldens stay #[ignore] until S2 regenerates them (fourth sanctioned move). | Dan |
| 2026-08-28 | WO-0008 S1 implementation decisions beyond the order's letter, each measured necessary to land Dan's four S1 gates at both seeds: (1) §3 condition 1 gains an absolute margin-span floor (SUTURE_ABS_CONTACT_KM = 5,500 km) — the perimeter-ratio test structurally vetoed giant-pair welds; (2) the suture/lock pair clocks decay at 2x on a lapse instead of hard-resetting (hex-boundary classification flicker, same hysteresis as the rift-onset clock); (3) amendment-B insulation anchors on max(last suture, last breakup) via new PlateState::youngest_breakup_my — a split vents the trapped heat; failed attempts vent nothing; ramp 55/165 My, refractory 180 My, K_MANTLE 0.10; (4) rift tips steer along the stress axis (away from the other tip) with weakness still gating each advance — halving cuts instead of margin slivers (the recorded m4 defect); a supercontinental plate may grow a second arm while the first is growing; (5) §6 fossil-boundary capture: a sub-2%-of-sphere plate whose whole boundary sits under the dead band for 60 My merges into its longest-border neighbor (Kula) — the split-debris death path; (6) same-plate continental inventory guard in advect (jam grind reverts excess losses). Aulacogen-style capping of failed-rift scars was tried and REVERTED (it froze seed cyrus; the S2 scar-maturation rule stands). MicroplateOrigin::Severed exists for the §7 fallback promotion. m2 armed at 2-6/Gy, m5b at <=10 cells/100My, s1 relic + area gates armed; m3 band and m1 pin stay recorded (42 splits 11/Gy; cyrus pin 530 My). | default |
| 2026-08-28 | WO-0008 S2 decisions: (1) continental-collision thickening creates NO volume — funded entirely by underthrust budgets (the consumed margin cell's whole column) deposited same-step into strength-limited inboard zones (W = 260 km/strength, craton-stopped, linear taper, exact integer units); 1/3 thickens the zone, 2/3 loads the pair's foreland shelf one full column at a time (consolidated platform-grade — age-0 forelands were weak-line attractors), which returns the consumed AREA; (2) spreading may flow onto same-plate shelf cells (orogenic collapse over the foreland); (3) arc sites use 3-ring clearance, not the order's 2-cell spacing — on a one-ring band 2-spacing mathematically caps at 50% band-land vs the order's own <30% target, and conversions are island-blocked so islands emerge one by one; (4) s2_island_arcs is RECORDED, not armed — advection smears/drifts sub-cell islands into small islets a runtime isolation test cannot tell from walls; the synthetic ocean-ocean test pins the strict production property; (5) m6 re-measured honestly (73%/39%) and gated at 34% per the order's honest-minus-five rule, ARMED; s2_orogen_width and s2_volume_ledger ARMED; (6) recalibration: C_CONTACT 900, refractory 240 My, MIN_SPLIT_CELLS 24, insulation 55/165 My — the full armed set green at both seeds. | default |
| 2026-08-28 | ALL THREE tectonic goldens regenerated — the fourth sanctioned golden move, WO-0008: S0 (whole-plate crust setup), S1 (closure/linkage/seam/balance) and S2 (distributed orogeny, discrete arcs, ledger) each change every step of every run from t = 0 — a whole-world change by design. Old values 0x2916_52b9_61e3_f74c / 0xa154_6f8c_7944_6284 / 0x08f5_c8f9_67db_bc04; new from print_tectonic_goldens on Daniels-MacBook-Air; the Phase 0 noise golden verified unmoved. | default |
| 2026-08-28 | Sea level: fixed water mass replaces constant ocean fraction (Dan 2026-08-28). Mass is conserved; volume is derived via seawater density. Hooks reserved for ice mass and thermal expansion (Phase 3+). | Dan |
| 2026-08-28 | WO-0009 hypsometry re-baseline (step 9): the conserved-water solve shifts the 500 My L7 seed-42 final-frame datum by ~+73 m — ocean mode -5360 -> -5287 m, land mode 202 -> 275 m, Ashman D 8.17 unchanged (old: docs/results/tectonics-wo0009-base-v0.2.3-Daniels-MacBook-Air.json, new: docs/results/tectonics-wo0009-Daniels-MacBook-Air.json). The gate thresholds (D > 2, ocean mode < -2500 m, land-mode magnitude < 1500 m) hold on both sides and stay unchanged. Also verified there: stability_cont_drift_rel 0.14 (> the 0.05 gate) and all_acceptance_pass=false are bit-identical on v0.2.3 base — the pre-existing continental-inventory leak, not this WO; sea level never feeds the dynamics. | default |
| 2026-08-28 | TECTONIC ELEVATION golden regenerated — the fifth sanctioned golden move, WO-0009, exactly once: elevation 0xabb5_3577_2c81_9c99 -> 0xaa15_8d4f_1563_9cd8. Sea level switched from the fixed t = 0 datum to a conserved water mass re-solved per keyframe (bisection, 40 fixed f64 iterations, serial cell-id-order mass integral), which shifts the elevation datum of every keyframe after t = 0 but touches no dynamics: plates 0x7680_b5da_d03d_486a and crust type 0xebe0_d5a5_a29e_2745 verified UNMOVED in the same run — exactly the datum-only signature (as in the 2026-08-19 datum move). Value from print_tectonic_goldens on Daniels-MacBook-Air; the phase-0 noise golden verified UNMOVED in the same suite run; the WO-0009 #[ignore] marker removed in the same change. | default |
| 2026-08-28 | WO-0011 S1 boundary regularization: `regularize_boundaries()` between `enforce_connectivity()` and `classify_boundaries()` — an advection re-sampling flip reverts unless the cell's lithosphere can fail (strength < N(STRENGTH_FAIL_MEAN=1.05, STRENGTH_FAIL_SIGMA=0.05), a deterministic per-(master seed, cell id, step index) FNV/splitmix draw: RNG-free in the reproducibility sense, keyframe-resume-safe) AND keeping it lowers the local strength-weighted boundary energy; craton floor judged on the previous column, with the consuming-trench exception and the `craton_transfer_violations` counter (S3 arms at zero). MEASURED deviations from the WO letter (all at L6 seed cyrus; full detail in code comments): (1) the process-edge exemption keys on recorded events (`outs.subducted`/`outs.collided`/hard-continent docking evidence) instead of the F_BND feature bits — F_BND_TRANSFORM is classify's dead-band default so the bit exemption made the pass a no-op (10 reverts/2 Gy), and the interleaved strips carry BOTH div+conv bits (~90% of finger cells), so any bit-based exemption protects the artifact itself; (2) sliver capture added: a cell holding to its plate by <=1 edge — or exactly 2 ring-consecutive (mutually adjacent, hence provably non-bridging) edges — with a clear foreign majority and an E-lowering transfer is captured, ownership only, crust stays; without it the finger population is STATIC debris (owner never changes -> never a candidate; 70-85% persisted per 100 My window) born of same-owner ridge gap-fill and weld scars, and the 2-edge flank case is what finally drains it; (3) reverts are connectivity-safe (must rejoin the previous plate; must not sever the new owner's ring-arc; capture never takes a plate's last cell) — naive reverts fed the SS7 backstop 430 cells/100 My vs the armed <=10, now 0; (4) continental-GAIN flips (ocean->continent) are exempt: the WO-0008 balance guard has already spent each gain to retain a trailing loss, and un-doing them bled continent (-23% over 2 Gy at seed 42; shipped: -9.6%). A tried pure-energy-minimum rule (E-lowering flips stand regardless of strength) measured WORSE (fingers 2.9%): weighted E trades short strong boundary for long weak boundary — the WO's AND-composition stands. RESULT (probe, 24 plates/land .40/vigor 1.0/2 Gy/seed cyrus): fingers <=0.2% at EVERY sample after 100 My (gate <=0.5% PASS); compact at 2 Gy = 0.94x its 100 My value, peak anywhere 1.08x (gate <=1.15x PASS at every sample, not just the endpoint); interior flips 0% at every post-transient sample. Gate re-baselines (WO-0008 S0 precedent; S2's weld rework re-measures, S3 re-arms; values in the results file): m1 stddev ARMED->RECORDED with /3 backstop; m1 pin backstop 2x->4x; m2 weld band and m7 mean-speed band ARMED->RECORDED with half/double backstops (the failing readings sit under wholesale-weld supercontinent states S2's front-limited welds will not build); relic-basin METRIC bounded to basin scale (<=1% of sphere — the >=80% enclosure test had matched the open world ocean when two continental giants bordered it). All armed gates green at both seeds; backstop worst window 0 cells; exclaves 0; volume ledger exact; continental area -7.7%/-9.6%. Tables: docs/results/plate-shape-wo0011-s1-Daniels-MacBook-Air.json. | default |
| 2026-08-28 | ALL THREE tectonic goldens regenerated — the sixth sanctioned golden move, WO-0011 S1+S2 together, exactly once (announced in WO-0011-S1): elevation 0xaa15_8d4f_1563_9cd8 -> 0xee3c_1341_0e60_7b05, plates 0x7680_b5da_d03d_486a -> 0xaa22_f87a_dacd_7acd, crust 0xebe0_d5a5_a29e_2745 -> 0x1a9b_7e90_1e25_4785 (print_tectonic_goldens on Daniels-MacBook-Air; Phase 0 noise golden verified UNMOVED in the same suite run). Whole-world change by design: S1's boundary regularization edits ownership flips every step, and S2 replaced the wholesale suture relabel with the front-limited weld — at §3 maturity the suture event/scar/counters fire exactly as before, but the loser now transfers progressively: a keyframed `Weld {winner, loser, fired_my, front_km, layers_done}`; the front advances WELD_FRONT_KM_MY = 50 km/My (India–Eurasia: ~2000 km into Asia in ~50 My; km not cells, so grid-level independent) as whole hop-layers via serial id-ordered BFS whose seeds are loser cells whose winner-side neighbor carries this weld's scar (`suture_at_my >= fired_my` — the scar advects with the material, anchoring the front to the sutured contact and never to far-side boundary segments the pair also shares; this replaces the WO's static "distance from the stamped contact cells", whose grid ids advection drifts off the material — with the loser motion-slaved to the winner the two rules are equal). Transferred cells still touching the loser stamp the scar (the scar advances with the front); bookkeeping per transferred cell; the crust-volume ledger is untouched (ownership relabels move no volume). Losers are slaved to the winner's pole/speed in motion_update, exempt from fossil capture (a slaved loser is quiet by construction; capturing it would recreate the dumbbell), and keep advecting, rifting, and running third-party pair timers. Remainder fragments severed by the front transfer to the winner (§7 by construction); at zero cells the weld retires the loser — slab ledger and remaining rifts to the winner exactly as the wholesale path — and chained welds re-point. Dumbbell probe committed (L7, 18° cap, off-centre 3-cell ~170 km contact forced to maturity): winner grows as one mass at every step — no sub-170 km neck joining two >2% masses (Dan's ruled bound; 2-ring-erosion component scan), clean run 42 steps, max 166 cells/step, max +2 boundary cells vs the wholesale ~4,000-cell/~200-rim-cell lump. Probe (24 plates/land .40/vigor 1.0/2 Gy/seed cyrus): largest share peaks 55.2% (was 67% pre-S1, 71% at S1) with alive 12–23; m2 sutures/Gy back INSIDE Dan's 2–6 band at BOTH seeds (2.5/2.5, was 1.5/2.0); m1 pin at cyrus 650 -> 360 My; all armed gates green at both seeds. Tables: docs/results/plate-shape-wo0011-s2-Daniels-MacBook-Air.json. S3 re-measures and re-arms the re-baselined clauses. | default |
| 2026-08-28 | WO-0011 S3 plate-shape gates ARMED at both seeds (crates/worldmaker-sim/tests/plate_shape_gates.rs; 2 Gy L6, 24 plates/land .40/vigor 1.0, run_history cadence): (1) compactness — Dan's ruled 1.15x (2 Gy vs 100 My) armed on the area-weighted `boundary_cell_fraction` (measured 0.81 cyrus / 1.06 seed 42); MEASURED deviation: the WO's unweighted `mean_boundary_per_area` is RECORDED instead of armed (1.22 / 1.82) — it is census-skewed, seed 42's healthy 17-21-plate churn reads WORSE (1.82) than the pre-fix world (1.52) while fingers sit at 0.3%, and total boundary length (the weighted form) is exactly what the gerrymander artifact multiplied; (2) fingers <= 0.5% at every sample after 100 My, armed as ruled (max 0.17% / 0.39%); (3) `craton_transfer_violations == 0` armed as ruled AFTER refining the counter to its design intent: instrumented attribution showed all 330 pre-refinement counts (both seeds) were recorded-process transfers — 324 continent-continent jam transfers (`outs.collided`, 188 at weld fronts; the exemption the regularization pass itself grants) and 6 sliver captures of severed craton fragments — zero from the flip machinery; write-only diagnostic, tectonic goldens verified unmoved; (4) largest-plate share: Dan's 40% hard cap RECORDED, armed at a 60% every-sample backstop (cyrus holds 52-55% from 800 My — the persistent-supercontinent state is the break-up item below, and pre-fix wholesale-weld worlds read 67-71%); the 40% cap re-arms with that item; (5) the 170 km neck clause armed on the S2 dumbbell world via `metrics::holds_narrow_neck` at NECK_MIN_KM=170/NECK_MASS_FRAC=0.02, stated in km (level-independent). m2 suture band RE-ARMED at Dan's 2-6/Gy in plate_physics_gates.rs (2.5/2.5 measured — the S1 promise settled); m1 stddev/pin and m7 mean-speed stay RECORDED on their backstops. Screenshots docs/media/wo-0011 (L7 cyrus, Dan's recording settings, 53/600/2000 My): PASS vs the original recording — no dumbbells, natural margins; one residual localized comb at 2000 My behind the fastest plate, inside the finger budget. Tables: docs/results/plate-shape-wo0011-s3-Daniels-MacBook-Air.json. | default |
| 2026-08-28 | WO-0011 S3 CENSUS FINDING (recorded, gate green): at 2 Gy from a 24-plate start, alive counts sit inside Dan's 8-20 band (12 cyrus / 17 seed 42) but the largest plate exceeds the 35% target at BOTH seeds — 55.2% cyrus (one unbroken supercontinent epoch from ~800 My to the end, 1.2 Gy without dispersal; 59.5% at L7 in the app) and 38.9% at seed 42. Welds assemble supercontinents faster than rifts break them: a separate break-up/rift balance item is RECOMMENDED (`check_rift_splits`, `grow_rifts`, `link_rifts`, micro thresholds — out of scope for WO-0011 by ruling). That item also re-arms the recorded 40% share cap, the m1 stddev (0.85/0.92 vs the 1.5 floor) and pin (360/620 My vs the 500 cap) clauses, and the m7 mean-speed band (1.67/2.35 cm/yr vs 2-6). | default |
