# WO-0003-S4: verify and close WO-0003

CONTEXT. Sessions 1–3 are merged. This session verifies, documents, and tags.

RULES. Single-track. Verification uses at most ten short subagents, one per area listed in step 2. If the usage limit nears: commit, push, update checkboxes, stop with a one-line note.

STEPS.

1. Capture the AFTER screenshots. Run the screenshot script with no explicit world flags; it forces seed cyrus, Standard7, Detail 1.0 to match the committed BEFORE set. Also capture: the coastline close-up at High8, and a plates-layer image at the same seed and era as the BEFORE plates image. Commit to `docs/media/feel-pass/`.
2. Verification pass, one short subagent per area: pending-edit guard; plate-generator gates; liveliness gates; render-only guard; Eckert IV round-trip and parity; preset defaults and CLI flags; golden integrity (only the two sanctioned moves happened; Phase 0 noise golden unmoved); CI configuration; WO-0003 checklist truth against the repo; report accuracy. Fix confirmed findings before closing.
3. Re-run the full workspace suite. Record in the work order that Sessions 1 and 3 moved no sim hash.
4. Check every WO-0003 box or mark it explicitly waived with a reason. Update the roadmap status. Delete merged branches.
5. Tag v0.2.1.
6. Report to Dan in plain language: each of the four fixes, how to see each one in the app (Detail slider, presets, Eckert IV, draw-then-Regenerate, a long history with no frozen plates), the headline numbers, and open questions for Phase 2.


FINAL LINE. After the report, print this block exactly, as the last output of the session. Print it only when every DONE WHEN condition holds. If the session stops early for any reason, print `NOT DONE` and the reason instead.

```
██████╗  ██████╗ ███╗   ██╗███████╗
██╔══██╗██╔═══██╗████╗  ██║██╔════╝
██║  ██║██║   ██║██╔██╗ ██║█████╗
██║  ██║██║   ██║██║╚██╗██║██╔══╝
██████╔╝╚██████╔╝██║ ╚████║███████╗
╚═════╝  ╚═════╝ ╚═╝  ╚═══╝╚══════╝
```
