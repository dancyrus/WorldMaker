# WO-0003-S3: pending edits — draw now, Regenerate applies

CONTEXT. The interface stubs exist: `crates/worldmaker-app/src/pending_edits.rs` and `crates/worldmaker-io/src/strokes.rs`. The overlay rendering hook is merged. The binding design is `docs/plan/feel-pass-design/d1-fix1-design.md`. The standing rule: drawing never starts a simulation.

RULES. Single-track. Checkpoint commits every 30–45 minutes. Branch: `feat/feel-pass-pending`. If the usage limit nears: commit, push, update checkboxes, stop with a one-line note.

STEPS.

1. Implement the pending stroke list per d1-fix1-design: an ordered list of strokes. Stroke payloads: CratonPaint (cell ids, +1 or −1) and HotspotAdd / HotspotRemove (unit-vector position).
2. Route the paint tools in `apply_tool` into the pending list. Remove every path from stroke handling to `Pipeline::run`. Add a test that proves the stroke path cannot start a simulation.
3. Show pending strokes through the merged overlay hook. The badge shows the stroke count.
4. Implement the commands: Ctrl+Z removes the newest pending stroke. Discard clears the list. Regenerate folds the strokes into `TectonicsParams::craton_overlay` and `TectonicsParams::hotspot_overlay`, clears the list, and runs history off-thread with the existing progress bar and cancel button.
5. Implement preset and seed semantics: a preset switch discards pending CratonPaint strokes and keeps pending Hotspot strokes. A seed change keeps all pending strokes. Record both rules in `docs/plan/decision-log.md`.
6. Derive serde Serialize and Deserialize on the stroke types in `worldmaker-io`. The save stubs stay stubs.
7. Keep these controls live and outside the pending system: sea level, layer, projection, timeline, Detail.
8. Run the workspace test suite. Confirm no sim golden moved.
9. Push, PR, merge when CI is green, delete the branch. Update the WO-0003 checkboxes. Report to Dan: how drawing feels now, and the one-line paste for Session 4.
